# Release v1 checklist — template

Status: BLOCKED BEFORE DEPLOYMENT.

Record each acceptance gate as pass/fail with release ID, evidence SHA-256,
reviewer identity, and UTC time. Verification and isolated restore attestations
must bind the same release and bundle hashes before promotion is considered.
